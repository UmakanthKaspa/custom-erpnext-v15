# ERPNext v15 + Healthcare + Payments
# Copy this file to .env and set your own passwords:
#   cp .env.example .env
#   nano .env

# Database root password (MariaDB)
DB_PASSWORD=change_me_to_strong_password

# ERPNext Administrator login password
ADMIN_PASSWORD=change_me_to_strong_password
